import React from 'react';
import MainMenu from '../MainMenu/MainMenu';

const Header = () => {
    return (
        <section>
            <MainMenu />
        </section>
    );
};

export default Header;