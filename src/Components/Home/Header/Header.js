import React from 'react';
import MainMenu from '../../Shared/MainMenu/MainMenu';

const Header = () => {
    return (
        <section>
            <MainMenu />
        </section>
    );
};

export default Header;